# Add real values only in backend-Py/.env (never commit .env)
GOOGLE_API_KEY=
RAPIDAPI_KEY=
LINKEDIN_USERNAME=
LINKEDIN_PASSWORD=
# Default true (headless). Set LINKEDIN_HEADLESS=false only when debugging in a visible browser.
LINKEDIN_HEADLESS=true
